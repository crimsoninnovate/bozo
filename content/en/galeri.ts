/**
 * Gallery page copy. See content/tr/galeri.ts for where each line comes from:
 * this page has no design file and no ready block in the copy inventory, so the
 * text is kept to the minimum and the missing pieces are reported rather than
 * invented. `aiNotu` is the same approved sentence as `menu.cekim.aiGorselNotu`.
 */
export const galeri = {
  baslik: 'Gallery',
  altMetin: 'Sixteen frames the site is waiting for',
  aiNotu: 'No AI generated food imagery is used. Slots stay dark until the shoot.',
}
